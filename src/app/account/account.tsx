"use client"

import { Header } from "@/components/header"
import { Box } from "@chakra-ui/react"

export const Account = () => {
    return (
        <Box>
            <Header />
        </Box>
    )
}