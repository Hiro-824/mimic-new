"use client"

import { Header } from "@/components/header"
import { Box } from "@chakra-ui/react"

export const Library = () => {
    return (
        <Box>
            <Header />
        </Box>
    )
}